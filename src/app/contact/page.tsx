export default function ContactPage() {
  return (
    <div className="mx-auto flex max-w-2xl flex-col gap-4 px-6 py-12">
      <h1 className="text-2xl font-semibold text-black dark:text-zinc-50">Contact</h1>
      <p className="text-zinc-600 dark:text-zinc-400">
        Placeholder — replace with a real phone/WhatsApp number and address
        before launch. Tenants use phone as their real contact channel per
        the dashboard schema, so a WhatsApp link here is the natural match.
      </p>
    </div>
  );
}
